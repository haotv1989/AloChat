import {  StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    /*
     * Removed for brevity
     */container: {
    flex: 1,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
  }
    
  });
