import {  StyleSheet } from 'react-native';
export default StyleSheet.create({
    /*
     * Removed for brevity
     */container: {
    flex: 1,
    padding: 12,
    flexDirection: 'row',
    alignItems: 'center',
  }
    
  });
